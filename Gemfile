source 'https://rubygems.org'

gem 'roda', '~> 3'                  # Lightweight web framework
gem 'thin', '~> 1', require: false  # Thin webserver
gem 'sass', '~> 3'                  # Sass style sheets
gem 'slim', '~> 4'                  # Slim template
gem 'uglifier', '~> 4'              # JS compressor
gem 'rake', '> 12'                  # Make for Ruby

group :development do
  gem 'shotgun', '~> 0.9'           # Code reloading
end

group :test do
  gem 'capybara', '~> 3'           # Code reloading
  gem 'minitest'#, '~> 3'           # Code reloading
  #gem 'capybara-webkit'
end

group :production do
end
